import 'package:flutter/material.dart';

class InsuranceViewBody extends StatelessWidget {
  const InsuranceViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
